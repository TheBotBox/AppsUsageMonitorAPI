<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bot.box.appusage">

    <application>
        <provider
            android:name="bot.box.appusage.provider.AppUsageProvider"
            android:authorities="${applicationId}.AppUsageProvider" />
    </application>
</manifest>
